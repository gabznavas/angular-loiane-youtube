import { FundoVermelhoDirective } from './fundo-vermelho.directive';

describe('FundoVermelhoDirective', () => {
  it('should create an instance', () => {
    const directive = new FundoVermelhoDirective();
    expect(directive).toBeTruthy();
  });
});
